import {LotType, Coordinate} from "../../types/lots"

function shortestLotPath(lot: LotType): Coordinate[] {
    for (let x in lot) {
        for (let y in lot) {
            if (lot[x][y] == )
        }
    }
}